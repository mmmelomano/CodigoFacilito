/*
def mi_funcion():
    x = 10  # Variable local dentro de la función
    print(x)

mi_funcion()  # Imprime 10

print(x)  # Error: la variable 'x' no está definida fuera de la función

x = 5  # Variable global

if x > 0:
    y = 2  # Variable local dentro del bloque 'if'
    print(y)

print(y)  # Imprime 2, la variable 'y' es accesible fuera del bloque 'if'

while x > 0:
    z = 3  # Variable local dentro del bloque 'while'
    print(z)
    x -= 1

print(z)  # Error: la variable 'z' no está definida fuera del bloque 'while'
*/

